 import type {post} from "@/app/lib/blog/definitions";
 import { blogPosts } from "@/app/ui/blog/post";

 export function fetchPostByPostTitile(postTitle:string) {
    // const ${postTitle}  = blogPosts().map((blogPost)=> (
    //     blogPost.postTitle
    // ))
    //  return (
  
    //  )
 }